# Diagnostic only: evaluate the completed v17 no-dither PPO checkpoint with a
# 1.50 s pulse instead of its trained 1.00 s pulse. This is deliberately a
# coarse duration ablation, not the intended event-conditioned final design.
experiment_config: applications/rl/configs/velocity_dodge_telegraph_v17_no_dither_event_pulse_residual_ppo.yaml

overrides:
  env:
    event_pulse_residual:
      pulse_kwargs:
        hold_steps: 30
